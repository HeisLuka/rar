name: Temporary focused rustfmt

on:
  push:
    branches:
      - "render-raster-contract-01"
    paths:
      - "crates/chaptera-render-raster-contract/**"
      - ".github/workflows/tmp-rustfmt-render-raster.yml"

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: "render-raster-contract-01"
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Format only task crate
        run: cargo fmt -p chaptera-render-raster-contract
      - name: Commit focused formatting
        run: |
          if git diff --quiet -- "crates/chaptera-render-raster-contract"; then
            echo "No formatting changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "crates/chaptera-render-raster-contract"
          git commit -m "RENDER-RASTER-CONTRACT-01: rustfmt focused crate"
          git push origin HEAD:render-raster-contract-01
